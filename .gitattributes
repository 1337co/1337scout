# Normalize line endings in the repository.
* text=auto

# Shell + Python MUST stay LF on every platform — a CRLF `.sh` breaks the
# `#!/usr/bin/env bash` shebang on macOS/Linux ("bad interpreter: ...^M").
*.sh  text eol=lf
*.py  text eol=lf
